# ===== EMPLOYEE VERIFICATION SYSTEM (EVS) FRONTEND - PRODUCTION =====
# Copy to .env.production and edit BEFORE running: npm run build
# EVS runs inside the unified HRMS Node backend at /api/evs - point this at
# the same backend domain every other portal uses.
VITE_API_URL=https://backend.your-domain.com/api/evs
